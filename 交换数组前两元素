import static java.util.Arrays.swap;

public class 数组交换前两个数 {
   
        public int[] exchangeAB ( int[] AB){
            AB[0] = AB[0] ^ AB[1];
            AB[1] = AB[1] ^ AB[0];
            AB[0] = AB[0] ^ AB[1];
            return AB;
        }
    }
